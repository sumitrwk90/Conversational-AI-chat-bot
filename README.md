### Conversational chat-bot

Main file is chat_with_memory.py to run for interactive session enjoyment with my chat-bot.

This chat-bot need very less resources to make:-
    1. gemini api key
    2. gradio for web interface
    
need to install dependencies:- run requirements.txt
run main file done...
